# Frontend Design

## Configuration

The configuration of the frontend is kept in a "config.json" file. The following sections describe the contents of the file.

### Servers and Players
* There is a list of available server URLs
    * Initial default entry: "http://localhost:3001"
* There is a list of available player URLs
    * Initial default entry: "http://localhost:3002"
